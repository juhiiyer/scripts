# usr/bin/python
#x = []
#for i in range(1,11):
	#x.append(2**i)

#print(x)

#list = [i for i in range(1,11)]
#print(list)
#x = [1, 2, 3, 4, 5, 6, False]
#print (all(x))
#x.append(8)
#print (x)
#y=[]
#z=[i for i in x if i not in y]
#	if i not in y:
#		y.append(i)

#print (x)
#print (z)		
#tc=()
#type(tc)

#tc =tuple("kimnamjun")
#type(tc)
#tc
#print (tc)
#tc = ([1, 2, 3, 4, 5])
#tc = (1, 2, 3, 4, [10, 20, 30, 40])
#print (tc[1:-3])
#index() returns index of given elelment in a tuple
#print(tc)
#print(tc.index(4))
#index(value)
#x=(1, 2, 3, 1, 2, 3, 1, 2, 3)
#len(x)
#we can do these things with tuples like mi, max, len etc
#print (sorted (t, reverse=False))//asc this one is wrong
#pw2=[]
#for i in 
#6)cmp(2.x)
# tuple has both positive and negetive somethoing...negetivve
#x = (1, 2, 3, 4, 5, 6, 1, 2, 5)
#for i in x:
	#print(i)
#for i in y:
	#print (i)
#print (y)
# A FEW THINGS ABOUT FUNCTIONS OF LISTS.OF
#LIST () THE CUNSTRUCTION OF A LIST FROM ANY ITRABLRE OBJECT.
# x = list ({1: 'x', 2: 'y'})
#x = 1, 2, 3, 4, 5
#print  (x)
# python takes it as a tuple. so, the output will be (1, 2, 3, 4, 5)
#x = 1, 2, 3, 4, 5, [1, 2, 3, 4]
#print(x)
# the output of this code is (1, 2, 3, 4, 5,[1, 2, 3, 4])
#if type(x)==tuple:
	#print('yes')

#x = (1, 2, 3, 4, 5,[10, 20, 30, 40])
#for i in x:
	#print(i)
	#if type(i)==list:
		#for t in i:
			#print(t)
		#else:
			#print(i)

# dictionaries!
# it is mutable 
# it is used to represent data in the form of key and valuse pairs.valuse
#keys must be uniqe but values can be duplicated.b
#insertation is not preserved.
#dynamic
#no indexing for dict and set.
#x={}
#type(x)
# separate key, value pair with colon
#d = {1: 'apple',}			
#we use key to perform operations on dict.
#2 ways to extract values from dict
#d = {1: 'apple', 2:'orange', 3:'pineapple'}
#print (d[1])
#prints apple
#d= {1:'rose', 2:'attipu', 3:'irish', 4:'orchids', 5:'marry gold'}
#for i in d:
	#str=d.get(i)
	#if str.startswith('a') or str.startswith('i') or str.startswith('u') or str.startswith('o'):
		#print(str)
# the output should be the flowers startingwith vowels
#easier way	
#if str.startswith((a,e,,i,o,u,A,E,I,O,U))	
#so much easier!
#keys()eturns list of keys.
#values()returns a list of values
#copy()shallow copy
#this is lecture 1